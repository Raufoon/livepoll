import React from 'react'
import LPForm from "../LPForm/LPForm";

const PollCreationForm = props => {
  return (
    <LPForm title={'Create a poll'} onSubmit={() => {}}>

    </LPForm>
  )
};

export default PollCreationForm